{
    "variables": {
        "aws_region": "{{env `AWS_REGION`}}",
        "aws_ami_name": "amazon-windows_{{isotime \"02Jan2006\"}}"
    },

    "builders": [{
        "type": "amazon-windows-ebs",
        "user_data_file": "./scripts/setup_winrm.ps1",
        "region": "{{user `aws_region`}}",
        "instance_type": "t2.micro",
        "winrm_username": "ec2-user",
        "ami_name": "{{user `aws_ami_name`}}",
        "ami_description": "Customized Windows",
        "associate_public_ip_address": "true",
        "communicator":"winrm",
        "winrm_use_ssl": true,
        "winrm_insecure": true,
        "source_ami" : "ami-f3dcbc8b"
    }],

    "provisioners": [
    ]
}